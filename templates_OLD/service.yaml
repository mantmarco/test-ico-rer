apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-api1-rer-sanita-laemur.fullname" . }}
  labels:
    {{- include "test-api1-rer-sanita-laemur.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-api1-rer-sanita-laemur.selectorLabels" . | nindent 4 }}
