apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-api1-rer-sanita-laemur.fullname" . }}-test-connection"
  labels:
    {{- include "test-api1-rer-sanita-laemur.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-api1-rer-sanita-laemur.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
